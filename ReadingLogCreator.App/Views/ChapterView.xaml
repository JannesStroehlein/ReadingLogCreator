<UserControl x:Class="ReadingLogCreator.App.Views.ChapterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:ReadingLogCreator.App.Views"
        mc:Ignorable="d"
        Width="400" Height="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <DockPanel LastChildFill="True">
            <TextBlock DockPanel.Dock="Left" Style="{StaticResource TextBlockSubTitle}">
                <TextBlock.Text>
                    <MultiBinding StringFormat="#{0}">
                        <Binding Path="Index"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            <TextBlock Text="{Binding Path=Title}" DockPanel.Dock="Right" Style="{Binding Path=Title}"/>
        </DockPanel>
    </Grid>
</UserControl>
